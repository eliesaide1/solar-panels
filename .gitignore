__pycache__/
*.py[cod]
.venv/
venv/

# Captured imagery and derived products can run to many GB
data/captures/
data/datasets/
data/outputs/
data/raw/

# Model checkpoints
models/*.pt

# Model artefacts and archived experiments
models/*.pkl
models/archive/
models/runs/
data/

# Never commit credentials
*.token
secrets.*

# Raw YOLO training checkpoints from the original run (~9 GB)
unet-training/

# ...but the working detector must ship with the code
!models/box_filter.pkl
