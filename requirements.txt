# Lower bounds, not exact pins. The versions noted alongside are the ones this
# project has actually been exercised against on Windows / Python 3.12.

# --- Core ---
numpy>=1.26          # verified 2.5.1
pillow>=10.0         # verified 12.2.0
pyyaml>=6.0
tqdm>=4.66
requests>=2.31       # verified 2.34.2

# --- Geo ---
rasterio>=1.3        # verified 1.5.0
shapely>=2.0         # verified 2.1.2
pyproj>=3.6          # verified 3.7.2

# --- API / UI ---
fastapi>=0.110       # verified 0.141.1
uvicorn[standard]>=0.30
pydantic>=2.0

# --- Model: only needed to train or detect, not to capture imagery ---
# CPU-only build:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.4                        # verified 2.13.0+cpu
torchvision>=0.19
segmentation-models-pytorch>=0.3  # verified 0.5.0
albumentations>=1.4               # verified 2.0.8 (2.x API is used)
opencv-python-headless>=4.9       # verified 5.0.0

# --- Google Earth Pro capture backend (Windows only, optional) ---
# pywin32>=306
